// use crate::config::Config;

// pub fn render_function(f: &syn::ItemFn, config: &Config, indent: String) -> String {
//     renderer(f, indent)
// }

// pub fn renderer(config: &Config, indent: String) {}
